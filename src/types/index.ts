export type NavLink = {
  label: string;
  href: string;
};

export type FeatureItem = {
  title: string;
  description: string;
  icon: string;
};

export type { ChatHistoryItem, ChatMessage, MessageRole } from "@/types/chat";
